package exam;

public class p3
{
//	for(int i=0; i<ranks.length; i++) { // 석차매기기 
//		for(int j=0; j<ranks.length; j++) {
//			if(total[i] < total[j]) {
//				ranks[i]++; // 총점이 다음 사람보다 적으면 내 등수가 1++
//				temp = ranks[i]; ranks[i] = ranks[j]; ranks[j] = temp; // 오름차순으로 정렬
//			}
//		}
//	}
}
