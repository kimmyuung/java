package Day06;

public class Car2 {
		// *클래스 이름 중복 X
	// 멤버 
	// 1. 필드 : 데이터가 저장되는 곳
	String color;
	// 2. 생성자 : 객체 생성시 초기화
		// 2-1 : 클래스이름 == 생성자이름
		// 주의 이름이 다를시 메소드 취급!
		// 2-2 : 외부로 들어오는 매개값(=인수), 필수는 아님
		// 2-3 : 리턴값(=반환값)
	
	Car2(){
		
	}; // 빈생성자(기본생성자) = 깡통 생성자 = 내용물이 없음
	
	// 필드가 있는 생성자 [인수가 존재]
	Car2(String color, int cc) {
		
	}
	
	// 3. 메소드 : 
}
